{
  "name": "typescript-missing-tests-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "coverage": "vitest run --coverage",
    "dry": "slophammer-ts dry .",
    "mutate": "stryker run",
    "check": "npm run format && npm run lint && npm run typecheck && npm run coverage && npm run dry && npm run mutate"
  }
}
