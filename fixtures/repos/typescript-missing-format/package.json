{
  "name": "typescript-missing-format-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "dry": "slophammer-ts dry .",
    "mutate": "stryker run",
    "check": "npm run lint && npm run typecheck && npm test && npm run coverage && npm run dry && npm run mutate"
  }
}
