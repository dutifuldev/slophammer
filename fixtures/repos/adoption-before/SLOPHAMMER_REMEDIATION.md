# Slophammer Remediation

This fixture represents the before state.

Expected remediation:

- Add `AGENTS.md`.
- Add GitHub Actions CI.
- Enable strict TypeScript compiler settings.
- Add ESLint rules that reject `any` and unsafe type operations.
- Add format, lint, typecheck, test, coverage, DRY, and mutation scripts.
- Add Slophammer config with explicit budgets.
