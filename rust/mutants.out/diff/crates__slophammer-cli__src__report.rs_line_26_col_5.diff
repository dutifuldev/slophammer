--- crates/slophammer-cli/src/report.rs
+++ replace write_json -> Result<String, ReportError> with Ok(String::new())
@@ -18,17 +18,17 @@
     Report {
         ok: findings.is_empty(),
         findings,
         scope: None,
     }
 }
 
 pub fn write_json(report: &Report) -> Result<String, ReportError> {
-    Ok(format!("{}\n", serde_json::to_string_pretty(report)?))
+    Ok(String::new()) /* ~ changed by cargo-mutants ~ */
 }
 
 pub fn write_text(report: &Report) -> String {
     if report.ok {
         return format!("OK: no findings\n{}", scope_line(report));
     }
     let mut output = String::new();
     for finding in &report.findings {
