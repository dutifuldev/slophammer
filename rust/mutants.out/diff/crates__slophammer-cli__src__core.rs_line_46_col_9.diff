--- crates/slophammer-cli/src/core.rs
+++ replace Finding::at_path -> Self with Default::default()
@@ -38,20 +38,17 @@
             severity: definition.severity,
             path: definition.path.to_owned(),
             message: definition.message.to_owned(),
             baselined: None,
         }
     }
 
     pub fn at_path(definition: &RuleDefinition, path: impl Into<String>) -> Self {
-        Self {
-            path: path.into(),
-            ..Self::new(definition)
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn with_message(
         definition: &RuleDefinition,
         path: impl Into<String>,
         message: impl Into<String>,
     ) -> Self {
         Self {
