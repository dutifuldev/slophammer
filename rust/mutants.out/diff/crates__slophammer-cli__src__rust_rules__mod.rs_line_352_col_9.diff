--- crates/slophammer-cli/src/rust_rules/mod.rs
+++ replace || with && in ignored_project_path
@@ -344,17 +344,17 @@
 fn rust_source_path(path: &str) -> bool {
     path.ends_with(".rs")
 }
 
 fn ignored_project_path(path: &str) -> bool {
     path.starts_with("fixtures/")
         || path.starts_with("templates/")
         || path.contains("/fixtures/")
-        || path.contains("/target/")
+        && /* ~ changed by cargo-mutants ~ */ path.contains("/target/")
         || path.contains("/node_modules/")
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::scan::{RepoFile, Snapshot};
     use std::collections::BTreeMap;
