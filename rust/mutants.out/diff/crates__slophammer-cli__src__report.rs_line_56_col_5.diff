--- crates/slophammer-cli/src/report.rs
+++ replace write_sarif -> Result<String, ReportError> with Ok(String::new())
@@ -48,20 +48,17 @@
             "scope: scanned {} of {} production files\n",
             scope.scanned, scope.production_files
         ),
         None => String::new(),
     }
 }
 
 pub fn write_sarif(report: &Report) -> Result<String, ReportError> {
-    Ok(format!(
-        "{}\n",
-        serde_json::to_string_pretty(&sarif_report(report))?
-    ))
+    Ok(String::new()) /* ~ changed by cargo-mutants ~ */
 }
 
 #[derive(Serialize)]
 struct SarifLog {
     #[serde(rename = "$schema")]
     schema: &'static str,
     version: &'static str,
     runs: Vec<SarifRun>,
