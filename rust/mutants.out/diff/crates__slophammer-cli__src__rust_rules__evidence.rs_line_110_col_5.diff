--- crates/slophammer-cli/src/rust_rules/evidence.rs
+++ replace normalized_content -> String with String::new()
@@ -102,26 +102,17 @@
 fn active_workflow_path(path: &str) -> bool {
     let Some(name) = path.strip_prefix(".github/workflows/") else {
         return false;
     };
     !name.contains('/') && (name.ends_with(".yml") || name.ends_with(".yaml"))
 }
 
 fn normalized_content(content: &str) -> String {
-    content
-        .lines()
-        .filter_map(strip_comment)
-        .collect::<Vec<_>>()
-        .join("\n")
-        .replace("\\\n", " ")
-        .split_whitespace()
-        .collect::<Vec<_>>()
-        .join(" ")
-        .to_ascii_lowercase()
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
 
 fn strip_comment(line: &str) -> Option<&str> {
     let trimmed = line.trim_start();
     if trimmed.starts_with('#') {
         return None;
     }
     Some(trimmed.split('#').next().unwrap_or(trimmed))
